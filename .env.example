# ===== 优选商城环境变量配置 =====
# 复制本文件为 .env 并按需修改（.env 已被 gitignore，不会提交）

# 服务端口（默认 3000）
PORT=3000

# JWT 签名密钥 —— 生产环境必须设置，否则服务拒绝启动
# 生成强随机密钥：openssl rand -base64 48
JWT_SECRET=

# 生产环境允许跨域访问 API 的来源（逗号分隔）。
# 不设置时生产环境仅允许同源访问（前后端同进程托管，通常无需设置）。
CORS_ORIGIN=